# PyCharm 机械臂抓球动画

右键运行 `pick_and_place.py`，即可看到机械臂靠近橙色小球、夹紧、抬起、搬运到绿色托盘、松开并归位。一次动作包含 20 秒仿真时间；完成后窗口会保留。

Windows 现在默认使用兼容显示窗口：仍由 MuJoCo 渲染三维画面，通过 Tk 显示，绕过原生 Viewer 的黑屏/花屏问题。空格暂停、R 重播；鼠标左键拖动旋转，滚轮缩放。本机已安装所需的 Tk 和 Pillow。

## 直接运行

1. 在 PyCharm 打开当前项目文件夹，或把下面两个文件按原目录结构一起复制到你的项目中：

   ```text
   你的项目/
   ├─ pick_and_place.py
   └─ models/
      └─ pick_and_place.xml
   ```

2. 选择你已经安装 `mujoco` 和 `matplotlib` 的 Python 解释器。本程序还使用 `numpy`，通常随这些包一起安装。
3. 右键 `pick_and_place.py` → **Run 'pick_and_place'**。无需填写参数，也无需下载机器人模型。
4. 在 3D 窗口内按 **空格**暂停或继续，按 **R**重新开始；关闭 3D 窗口后，程序保存结果并结束。请先点击 3D 窗口，让它获得键盘焦点。

模型和默认输出目录都由脚本自身位置定位，支持中文项目路径，不依赖 PyCharm 的工作目录设置。新增程序独立运行，原有项目脚本保持不变。

## 可选运行方式

在 PyCharm 的运行配置中，把以下选项填入脚本参数；也可以在项目终端运行对应命令。

| 命令 | 用途 |
| --- | --- |
| `python pick_and_place.py` | 打开 3D 抓球动画 |
| `python pick_and_place.py --display tk` | 明确选择兼容显示窗口，适用于原生窗口黑屏/花屏 |
| `python pick_and_place.py --display native` | 使用 MuJoCo 原生窗口 |
| `python pick_and_place.py --speed 0.5` | 慢速播放；速度范围为 0.1～4 |
| `python pick_and_place.py --plot` | 关闭 3D 窗口后弹出 Matplotlib 轨迹图 |
| `python pick_and_place.py --headless` | 无窗口完成一次物理仿真并保存结果 |
| `python pick_and_place.py --headless --gif` | 导出抓球 GIF 和预览图 |
| `python export_video.py` | 导出 20 秒、1080p、30 fps 的完整 MP4 视频 |

`--gif` 会直接进入离屏渲染模式，仍需要可用的 OpenGL 图形环境，并使用 Pillow（导入名为 `PIL`）。普通 `--headless` 不创建渲染器。

兼容窗口也使用 OpenGL 离屏渲染，解决的是原生交互窗口的显示兼容问题，不能替代缺失的 OpenGL 环境。如果看见旧的黑屏窗口，请先关闭它，再重新运行脚本，旧进程不会自动加载代码更新。

MP4 保存到 `outputs/pick_and_place/video/robot_pick_and_place.mp4`，使用 H.264 编码，无音轨。当前项目已准备好 `.video_dependencies` 中的编码器；如果把导出脚本复制到其他电脑，需同时保留 `pick_and_place.py` 和模型目录，并安装 `imageio-ffmpeg` 或准备 PATH 中的 ffmpeg。

结果保存在脚本同级的 `outputs/pick_and_place/`：

- `result.json`：抓取是否成功、抬升高度、放置误差等指标。
- `trajectory.csv`：球与夹爪的运动记录。
- `trajectory.png`：高度曲线和俯视轨迹图。
- 使用 `--gif` 后另有 `pick_and_place.gif`、`preview.png`。

提前关闭窗口也会保存当前记录，此时 `completed` 为 `false`。按 R 重播会重新记录，最终保存最后一次动作的数据。

## 如果提示缺少模块

先检查 PyCharm 当前项目的 Python 解释器，确保它就是已安装这些包的环境。仅在缺包或版本不兼容时，在该解释器对应的终端安装：

```text
python -m pip install mujoco==3.3.7 numpy matplotlib pillow
```

本项目使用官方 `mujoco` Python API，按 MuJoCo 3.3.7 进行验证。若终端与 PyCharm 指向不同解释器，应使用 PyCharm 解释器设置中的包管理界面安装到当前项目环境。

## 仿真模型

这是用于演示抓取过程的简化四关节机械臂，末端带两根滑动手指，采用位置伺服和理想重力补偿。球是自由运动刚体，抓取依靠 MuJoCo 计算的真实接触与摩擦；没有把球焊接到夹爪，也没有在运动过程中手工修改球的位置。

官方参考：[MuJoCo Python API 与交互窗口](https://mujoco.readthedocs.io/en/3.3.7/python.html)、[MuJoCo XML 模型配置](https://mujoco.readthedocs.io/en/3.3.7/XMLreference.html)。
