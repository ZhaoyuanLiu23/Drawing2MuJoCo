# 开发约定

1. 根据几何、文字和约束解析特征。禁止按零件名称、文件名、样例尺寸或数量分派。
2. 新能力须有独立 synthetic 图纸验证，并保持 B1/B2/B3 回归。
3. 输入尺寸/测试期望可出现在案例中，不可成为生产代码默认值或 fallback。
4. `nominal` 只表示明确名义值。中值、几何估算与位置推断须记录来源、证据、置信度。
5. 保留冲突及未知单位，不能靠改写原始值或猜单位解决。
6. CAD 与 MuJoCo 分开安装；保留既有抓取行为。
7. 不提交虚拟环境、凭据、缓存、第三方可执行文件或全部输出；精选案例说明来源并去除个人绝对路径。

测试见 [docs/VALIDATION.md](docs/VALIDATION.md)。提交说明包含问题、最终行为、验证和限制。当前是私密项目，不自动发布到公共 Pages 或公开模型库。
